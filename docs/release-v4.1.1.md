# NewAgent 4.1.1

- Windows installer повторяет атомарный rename при кратковременной блокировке файлов антивирусом или индексатором.
- Состояние установки использует тот же защищённый retry и не остаётся частично обновлённым.
- Полный SQLite-граф 0.5.0 загружается из отдельного проверяемого GitHub Release asset.
