# Выпуск 0.1.0

## Готово

- консольное меню и команды;
- режимы `flexible` и `controlled`;
- проекты в отдельных папках и история версий;
- обязательные апрувы и отзыв успешности;
- реестр только подтверждённых примеров;
- маршрутизация XML/документации/веб-источников по конфигурации и релизу;
- профили Codex, Claude Code, OpenCode, OpenAI API и совместимых API/CLI;
- экспорт Markdown, JSON и HTML;
- универсальный валидатор инструкций;
- Windows-сборка.

## Локальная сборка Windows

- файл: `dist/consultant.exe`;
- версия: `0.1.0`;
- размер: `8 208 815` байт;
- SHA-256: `0F6E3C0D449D6C3E172CE5733369373AEEE1661DFA1DC34BB36CFD5ED8C8E35A`.

`dist/` исключён из Git; сборка воспроизводится командой `./scripts/build_windows.ps1`.

## Проверки

- 8 модульных тестов: пройдены;
- валидация репозитория: пройдена;
- 6 переносимых скиллов: валидны;
- контрольный запрос XML-индекса по документу `ЗаказПоставщику`: пройден;
- прямой запуск, русская кодировка и встроенная валидация `consultant.exe`: пройдены.

Codex Desktop обнаруживается в текущей системе, но его внутренний executable WindowsApps недоступен для терминального запуска (`WinError 5`). Это не мешает работать в уже открытом Codex с папкой проекта; для генерации из самого `consultant` нужен отдельно доступный CLI или API-профиль.
